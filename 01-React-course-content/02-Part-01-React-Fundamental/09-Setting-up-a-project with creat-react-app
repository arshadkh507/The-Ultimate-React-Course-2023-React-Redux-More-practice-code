section-02 setting up new project with create-react-app

create react app is a command line interface tool
and now we need actually a terminal or a command prompts as it's called
on windows.

open VScode and open the dirctory where you want to do this project
